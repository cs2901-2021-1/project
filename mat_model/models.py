import tensorflow
import pandas

class models(object):
    """Model training and usage"""
    def __init__(self):
        pass

    def train(self, students_path: str, enrollment_path: str):
        print(f"TODO train {students_path} {enrollment_path}")

    def query(self, course_id: str, students_path: str):
        print(f"TODO query {course_id} {students_path}")
