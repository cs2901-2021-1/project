import tensorflow
import pandas

class models(object):
    """Model training and usage"""
    def __init__(self):
        pass

    def train(self, course_id: str, students_path: str):
        print(f"TODO train {course_id} {students_path}")

    def predict(self, course_id: str, students_path: str):
        print(f"TODO query {course_id} {students_path}")
